post:
  tags:
    - Resumes
  summary: Create tailored resume
  description: Create tailored resume
  operationId: createTailoredResumes
  responses:
    200:
      description: Successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/resumes/Resume.yaml
    401:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/errors/ErrorResponse.yaml
    500:
      $ref: ../../components/responses/InternalServerError.yaml

get:
  tags:
    - Resumes
  summary: Get tailored resumes
  description: Get tailored resumes
  operationId: getTailoredResumes
  responses:
    200:
      description: Successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/resumes/Resume.yaml
    401:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/errors/ErrorResponse.yaml
    500:
      $ref: ../../components/responses/InternalServerError.yaml
