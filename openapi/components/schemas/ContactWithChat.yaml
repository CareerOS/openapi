type: object
properties:
  contact:
    $ref: ./Contact.yaml
  chat_id:
    type: string
    format: uuid
    description: Chat ID associated with the contact
  outlook_chat_id:
    type: string
    format: uuid
    description: Outlook chat ID associated with the contact
  company_id:
    type: string
    format: uuid
    description: ID of the company associated with the contact
required:
  - contact
