type: object
properties:
  university_name:
    type: string
    description: The name of the university where the current user studies.
  cohort_name:
    type: string
    description: The name of the cohort of the university.
  students:
    $ref: './StudentWithQuests.yaml'
